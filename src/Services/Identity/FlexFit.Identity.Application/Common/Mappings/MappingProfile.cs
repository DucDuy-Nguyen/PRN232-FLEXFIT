using AutoMapper;
using FlexFit.Identity.Domain.Entities;

namespace FlexFit.Identity.Application.Common.Mappings;

public sealed class MappingProfile : Profile
{
    public MappingProfile()
    {
        // Define any AutoMapper mappings here if needed
        // (Currently returning record types, so map configs can be expanded later)
    }
}
